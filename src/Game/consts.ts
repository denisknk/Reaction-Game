export const gameConditions = {
  menu: 'menu',
  timer: 'timer',
  action: 'action',
  end: 'end'
};

export enum GameConditions {
  Menu = 'menu',
  Timer = 'timer',
  Action = 'action',
  End = 'end'
}
